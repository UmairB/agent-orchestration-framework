# GSD SDK Fixtures

These fixtures seed the Phase 36 `MockGSDTools` harness.

## v17-active

Captured from this repository while v1.7 Phase 33 was discussed but not yet planned:

```powershell
node .codex\get-shit-done\bin\gsd-tools.cjs roadmap analyze --raw
node .codex\get-shit-done\bin\gsd-tools.cjs state load --raw
```

Files:

- `v17-active/roadmap-analyze.stdout.json`
- `v17-active/state-load.stdout.txt`

