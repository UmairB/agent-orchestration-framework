---
phase: 01-aof-workspace-model
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/model.mjs
  - src/dsl.mjs
  - src/adapters.mjs
  - schemas/aof.schema.json
  - test/model.test.mjs
  - test/adapters.test.mjs
  - test/integration/cli.feature
  - test/integration/cli.mjs
  - scripts/test-unit.mjs
requirements:
  - ASST-01
  - ASST-02
  - ASST-03
  - ASST-04
  - ASST-05
  - RTOV-01
  - RTOV-02
  - RTOV-03
autonomous: true
user_setup: []
must_haves:
  truths:
    - "D-18, D-19, and D-20: AOF has one central source for resource kinds, runtime IDs, default asset filenames, and runtime capability metadata."
    - "D-02, D-09, D-10, D-14, and D-15: Skills, commands, agents, and natural-language rules can be represented as file-backed .aof assets with metadata paths."
    - "ASST-05: Assets can declare Claude and Codex runtime targets."
    - "D-03 and D-16: Runtime override files live beside assets and can change allowed metadata/rendering fields but cannot change asset identity."
    - "D-21: Override data is shallow-merged with shared asset metadata."
    - "D-22 and D-23: File-backed assets, capabilities, and runtime overrides are covered by BDD scenarios."
  artifacts:
    - path: "src/model.mjs"
      provides: "Central resource kind, runtime, capability, and override model"
      exports: ["RESOURCE_KINDS", "RUNTIMES", "CAPABILITIES"]
    - path: "schemas/aof.schema.json"
      provides: "Schema support for .aof resources, rule kind, paths, and overrides"
      contains: "rule"
    - path: "test/model.test.mjs"
      provides: "Unit tests for capabilities and override merge policy"
      contains: "shallow"
  key_links:
    - from: "src/dsl.mjs"
      to: "src/model.mjs"
      via: "resource/runtime validation and override merge helpers"
      pattern: "from \"./model\\.mjs\""
    - from: "src/adapters.mjs"
      to: "src/model.mjs"
      via: "runtime definitions and resource kind constants"
      pattern: "from \"./model\\.mjs\""
---

<objective>
Create the central AOF model for resource kinds, runtime capabilities, file-backed asset metadata, and runtime overrides.

Purpose: Make `.aof/` parsing and future rendering/UI behavior derive from one source of truth instead of duplicated constants.
Output: Central model module, parser/schema updates, override merge behavior, and tests.
</objective>

<execution_context>
@C:/Source/umair/aof/.codex/get-shit-done/workflows/execute-plan.md
@C:/Source/umair/aof/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-aof-workspace-model/01-CONTEXT.md
@.planning/phases/01-aof-workspace-model/01-RESEARCH.md
@.planning/phases/01-aof-workspace-model/01-01-SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@src/dsl.mjs
@src/adapters.mjs
@schemas/aof.schema.json
@test/adapters.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add central model and capability table</name>
  <files>src/model.mjs, src/dsl.mjs, src/adapters.mjs, test/model.test.mjs, scripts/test-unit.mjs</files>
  <read_first>.planning/phases/01-aof-workspace-model/01-CONTEXT.md, src/dsl.mjs, src/adapters.mjs, test/adapters.test.mjs, scripts/test-unit.mjs</read_first>
  <action>Create `src/model.mjs` as the central source for `RUNTIMES` (`claude`, `codex`), `RESOURCE_KINDS` (`skill`, `command`, `agent`, `rule`), default body filenames (`SKILL.md`, `COMMAND.md`, `AGENT.md`, `RULE.md`), and capability entries for `skill`, `command`, `agent`, `rule`, `pathScopedRule`, and future `codexExecutionPolicyRule`. Each capability entry must state per-runtime status such as `native`, `mapped`, `unsupported-warning`, `unsupported-fail`, or `future`. Refactor `src/dsl.mjs` and `src/adapters.mjs` to import runtime/kind constants from `src/model.mjs` instead of hard-coded sets where practical. Add `test/model.test.mjs` and register it in `scripts/test-unit.mjs`.</action>
  <verify>npm run test:unit</verify>
  <acceptance_criteria>
    - `src/model.mjs` exports `RESOURCE_KINDS`, `RUNTIMES`, `CAPABILITIES`, and default filename data.
    - `src/dsl.mjs` accepts `rule` as a valid resource kind.
    - Unit tests prove the capability table distinguishes Codex natural-language guidance from future Codex execution-policy rules.
    - Existing adapter tests still pass.
  </acceptance_criteria>
  <done>Runtime and resource capability data has one code source of truth.</done>
</task>

<task type="auto">
  <name>Task 2: Parse file-backed asset metadata and runtime overrides</name>
  <files>src/dsl.mjs, src/model.mjs, test/model.test.mjs, test/adapters.test.mjs, test/integration/cli.feature, test/integration/cli.mjs</files>
  <read_first>src/dsl.mjs, src/fs.mjs, .planning/phases/01-aof-workspace-model/01-CONTEXT.md, test/integration/cli.feature</read_first>
  <action>Extend config/resource resolution so `.aof/aof.config.json` can reference file-backed assets under `.aof/assets/<kind>/<id>/...` using `path`. Add support for optional asset `overrides` metadata or conventional `overrides/<runtime>.json` files beside the asset body. Implement shallow merge of shared metadata plus the selected runtime override. Reject override attempts to change `id` or `kind` with a clear error. Support shared defaults (`description`, `body` or body loaded from `path`, `model`, `tools`, `paths`) plus Claude/Codex-specific override fields. Add BDD scenarios for file-backed body loading, allowed runtime override, and invalid identity override failure.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - File-backed `.aof/assets/skills/<id>/SKILL.md` bodies are loaded through config `path`.
    - Runtime override JSON is read from the asset directory and shallow-merged.
    - An override containing a different `id` or `kind` fails with a clear message.
    - BDD scenarios exist for file-backed loading and override behavior.
    - `npm test` passes.
  </acceptance_criteria>
  <done>Assets and runtime overrides are parsed according to the Phase 1 context decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Align schema with .aof model</name>
  <files>schemas/aof.schema.json, test/model.test.mjs, test/integration/cli.feature</files>
  <read_first>schemas/aof.schema.json, src/model.mjs, .planning/phases/01-aof-workspace-model/01-CONTEXT.md</read_first>
  <action>Update `schemas/aof.schema.json` to allow the `rule` kind, `paths` on resources, file-backed `path`, runtime target arrays, package declarations, and runtime override metadata. Keep `additionalProperties: true` only where necessary for forward-compatible metadata; tighten core fields enough to catch invalid runtime IDs and invalid resource kinds. Add tests that validate representative `.aof/aof.config.json` examples if the project already has schema validation helpers; otherwise add lightweight JSON-schema coverage only if a validator is already available. Do not add a new dependency solely for schema tests in this phase without a clear reason.</action>
  <verify>npm run test:unit && npm test</verify>
  <acceptance_criteria>
    - Schema includes `rule` in resource kind enum.
    - Schema includes `paths` for rule/path-scoped guidance.
    - Schema examples or tests cover `.aof/aof.config.json` shape.
    - Existing tests pass without adding unnecessary dependencies.
  </acceptance_criteria>
  <done>Schema supports the central `.aof` asset model.</done>
</task>

</tasks>

<verification>
- [ ] `npm run test:unit`
- [ ] `npm test`
- [ ] BDD scenarios cover file-backed asset loading and runtime override behavior.
- [ ] Unit tests cover central capability table and override merge rules.
</verification>

<success_criteria>
- Central model definitions exist and are used by parser/adapter code.
- `skill`, `command`, `agent`, and `rule` are valid AOF resource kinds.
- Shared defaults and Claude/Codex runtime overrides are represented and parsed.
- Override merge is shallow and identity fields cannot be changed.
- All new functionality has BDD coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/01-aof-workspace-model/01-02-SUMMARY.md`.
</output>
