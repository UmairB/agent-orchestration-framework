---
phase: 01-aof-workspace-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli.mjs
  - src/dsl.mjs
  - src/fs.mjs
  - src/workspace.mjs
  - test/integration/cli.feature
  - test/integration/cli.mjs
  - test/workspace.test.mjs
  - scripts/test-unit.mjs
requirements:
  - WORK-01
  - WORK-02
  - WORK-03
autonomous: true
user_setup: []
must_haves:
  truths:
    - "D-01: A new project can be initialized with .aof/aof.config.json as the primary config file."
    - "D-04: A new project records lock/install state under .aof/aof.lock.json."
    - "D-05: .aof/aof.config.json is authoritative when both .aof and legacy root configs exist."
    - "D-06: A project with a legacy root aof.config.json is not silently migrated by aof init."
    - "D-07 and D-08: A user can explicitly run aof migrate to create .aof/ from a legacy root config without mutating the root file."
    - "D-22 and D-23: New workspace and migration functionality is covered by BDD scenarios."
  artifacts:
    - path: "src/workspace.mjs"
      provides: "Central workspace path and config discovery helpers"
      exports: ["workspacePaths", "findProjectConfig", "isLegacyConfigOnlyProject"]
    - path: "test/integration/cli.feature"
      provides: "BDD coverage for .aof init and migration behavior"
      contains: "aof migrate"
    - path: "src/cli.mjs"
      provides: "CLI support for .aof init, config precedence, and explicit migration"
      contains: "migrate"
  key_links:
    - from: "src/cli.mjs"
      to: "src/workspace.mjs"
      via: "workspace/config discovery helpers"
      pattern: "from \"./workspace\\.mjs\""
    - from: "test/integration/cli.feature"
      to: "src/cli.mjs"
      via: "black-box CLI scenarios"
      pattern: "Scenario: .*migrate"
---

<objective>
Implement the `.aof/` workspace entry point, config precedence, explicit migration command, and lock file relocation.

Purpose: Establish the source-of-truth workspace boundary before changing deeper asset semantics.
Output: `.aof/` workspace creation/discovery, `aof migrate`, and BDD coverage proving non-destructive legacy reconciliation.
</objective>

<execution_context>
@C:/Source/umair/aof/.codex/get-shit-done/workflows/execute-plan.md
@C:/Source/umair/aof/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-aof-workspace-model/01-CONTEXT.md
@.planning/phases/01-aof-workspace-model/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@src/cli.mjs
@src/dsl.mjs
@src/fs.mjs
@test/integration/cli.feature
@test/integration/cli.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add workspace path and config discovery helpers</name>
  <files>src/workspace.mjs, src/dsl.mjs, test/workspace.test.mjs, scripts/test-unit.mjs</files>
  <read_first>src/dsl.mjs, src/fs.mjs, test/paths.test.mjs, scripts/test-unit.mjs, .planning/phases/01-aof-workspace-model/01-CONTEXT.md</read_first>
  <action>Create `src/workspace.mjs` with central helpers for `.aof` workspace paths. Include `workspacePaths(projectDir)` returning absolute paths for `.aof/`, `.aof/aof.config.json`, `.aof/aof.lock.json`, and `.aof/assets`. Include `legacyConfigPath(projectDir)`, `findProjectConfig(projectDir, explicitConfigPath)` that honors explicit `--config`, otherwise prefers `.aof/aof.config.json`, then falls back to root `aof.config.json`, and `isLegacyConfigOnlyProject(projectDir)`. Update `src/dsl.mjs` loading call sites only as needed to accept discovered config paths; keep existing `loadConfig(configPath)` behavior intact. Add unit tests for config precedence, explicit config override, and legacy-only detection. Register `test/workspace.test.mjs` in `scripts/test-unit.mjs`.</action>
  <verify>npm run test:unit</verify>
  <acceptance_criteria>
    - `src/workspace.mjs` exports `workspacePaths`, `legacyConfigPath`, `findProjectConfig`, and `isLegacyConfigOnlyProject`.
    - Unit tests prove `.aof/aof.config.json` wins over root `aof.config.json` when no explicit `--config` is passed.
    - Unit tests prove explicit config path wins over both discovered locations.
    - `npm run test:unit` passes.
  </acceptance_criteria>
  <done>Workspace discovery is centralized and covered by unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Move init output into .aof for new projects</name>
  <files>src/cli.mjs, src/fs.mjs, test/integration/cli.feature, test/integration/cli.mjs</files>
  <read_first>src/cli.mjs, src/fs.mjs, src/catalog.mjs, src/adapters.mjs, test/integration/cli.feature, test/integration/cli.mjs</read_first>
  <action>Update `aof init` so new project configuration is written to `.aof/aof.config.json` and install lock state is written to `.aof/aof.lock.json`. Ensure parent directories are created through existing `writeText`. Preserve existing rendering behavior to `.claude/` and `.codex/` for now. If root `aof.config.json` exists and `.aof/aof.config.json` does not, make `aof init` fail with a clear message that includes `Run aof migrate`. Update BDD scenarios that previously expected root `aof.config.json` and root `aof.lock.json` for new init behavior. Extend BDD step support only if needed to assert JSON files under nested paths.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - New init scenarios assert `.aof/aof.config.json` exists.
    - New init scenarios assert `.aof/aof.lock.json` contains selected items and runtimes.
    - A BDD scenario proves `aof init` fails when only legacy root `aof.config.json` exists and stderr contains `aof migrate`.
    - Existing dry-run behavior still does not write `.aof/aof.config.json`, `.aof/aof.lock.json`, or runtime output files.
    - `npm test` passes.
  </acceptance_criteria>
  <done>`aof init` creates the `.aof/` workspace for new projects and refuses silent legacy migration.</done>
</task>

<task type="auto">
  <name>Task 3: Add explicit non-destructive aof migrate command</name>
  <files>src/cli.mjs, test/integration/cli.feature, test/integration/cli.mjs, README.md</files>
  <read_first>src/cli.mjs, src/dsl.mjs, src/adapters.mjs, test/integration/cli.feature, README.md</read_first>
  <action>Add top-level `aof migrate` command. It should require a legacy root `aof.config.json`, fail if `.aof/aof.config.json` already exists unless a clear force flag is intentionally implemented, read and normalize the legacy config through existing DSL logic, write `.aof/aof.config.json`, write `.aof/aof.lock.json` if enough selected item/package/runtime data exists or a minimal reproducible migration lock if not, and leave the root `aof.config.json` untouched. Output must warn that `.aof/aof.config.json` is now authoritative and root `aof.config.json` is legacy. Add BDD scenarios for successful migration, root file preservation, and `.aof` precedence after migration. Update help text and README with `aof migrate` usage.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - `aof migrate` succeeds from a legacy root config and creates `.aof/aof.config.json`.
    - BDD asserts root `aof.config.json` still exists and is not replaced by a pointer file.
    - BDD asserts stdout includes `.aof/aof.config.json is now authoritative` or equivalent clear wording.
    - Help text includes `aof migrate`.
    - `npm test` passes.
  </acceptance_criteria>
  <done>Legacy root projects have an explicit, non-destructive migration path.</done>
</task>

</tasks>

<verification>
- [ ] `npm run test:unit`
- [ ] `npm test`
- [ ] BDD scenarios cover new init path, init refusal on legacy-only config, and successful explicit migration.
</verification>

<success_criteria>
- `.aof/aof.config.json` is the default config for new projects.
- `.aof/aof.lock.json` is the default lock state for new projects.
- Root `aof.config.json` remains readable as legacy config but loses authority when `.aof/aof.config.json` exists.
- Explicit `aof migrate` exists and does not mutate root config.
- All new user-facing behavior has BDD coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/01-aof-workspace-model/01-01-SUMMARY.md`.
</output>
