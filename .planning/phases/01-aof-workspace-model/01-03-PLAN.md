---
phase: 01-aof-workspace-model
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/adapters.mjs
  - src/model.mjs
  - src/cli.mjs
  - schemas/aof.schema.json
  - README.md
  - test/adapters.test.mjs
  - test/integration/cli.feature
  - test/integration/cli.mjs
requirements:
  - WORK-01
  - WORK-02
  - ASST-01
  - ASST-02
  - ASST-03
  - ASST-04
  - ASST-05
  - RTOV-01
  - RTOV-02
  - RTOV-03
autonomous: true
user_setup: []
must_haves:
  truths:
    - "D-11: Claude natural-language rule assets have a defined mapping to .claude/rules/*.md."
    - "D-12: Codex natural-language rule assets have a defined mapping to AGENTS.md or nested AGENTS.md."
    - "D-13 and D-17: Codex .codex/rules/*.rules is documented as a separate future execution-policy asset type and capability behavior is handled one-by-one."
    - "D-01 through D-04 and D-07 through D-08: README documents .aof/aof.config.json, .aof/aof.lock.json, assets, overrides, and migration."
    - "D-22 and D-23: Phase 1 plans and docs retain BDD coverage as a required verification standard."
  artifacts:
    - path: "src/adapters.mjs"
      provides: "Runtime-aware handling for rule asset model"
      contains: "rule"
    - path: "README.md"
      provides: ".aof workspace and migration documentation"
      contains: ".aof/aof.config.json"
    - path: "test/integration/cli.feature"
      provides: "BDD coverage for rule model and runtime capability behavior"
      contains: "rule"
  key_links:
    - from: "src/adapters.mjs"
      to: "src/model.mjs"
      via: "capability-aware rule rendering decisions"
      pattern: "CAPABILITIES|RESOURCE_KINDS|rule"
    - from: "README.md"
      to: ".planning/phases/01-aof-workspace-model/01-CONTEXT.md"
      via: "documented user decisions"
      pattern: "\\.aof/assets"
---

<objective>
Finish Phase 1 by aligning runtime rule semantics, documentation, final BDD coverage, and planning state.

Purpose: Ensure the workspace model is coherent for Claude/Codex guidance before Phase 2 expands rendering and lock reproducibility.
Output: Rule mapping behavior, docs, final tests, ROADMAP/STATE planning annotations.
</objective>

<execution_context>
@C:/Source/umair/aof/.codex/get-shit-done/workflows/execute-plan.md
@C:/Source/umair/aof/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-aof-workspace-model/01-CONTEXT.md
@.planning/phases/01-aof-workspace-model/01-RESEARCH.md
@.planning/phases/01-aof-workspace-model/01-02-SUMMARY.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/TESTING.md
@src/adapters.mjs
@src/model.mjs
@README.md
@test/integration/cli.feature
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement minimal rule mapping behavior for Phase 1</name>
  <files>src/adapters.mjs, src/model.mjs, test/adapters.test.mjs, test/integration/cli.feature, test/integration/cli.mjs</files>
  <read_first>src/adapters.mjs, src/model.mjs, test/adapters.test.mjs, .planning/phases/01-aof-workspace-model/01-CONTEXT.md</read_first>
  <action>Add Phase 1 support for the shared natural-language `rule` asset kind. Claude rendering should map `rule` assets to `.claude/rules/<id>.md` and preserve `paths` frontmatter when provided. Codex rendering should map non-path-scoped `rule` assets into `AGENTS.md` content or another explicit Codex guidance strategy documented in code comments/tests; if nested/path-scoped Codex `AGENTS.md` generation is too broad for Phase 1, implement the parser/model behavior and emit an explicit warning/status for mapped support while leaving full nested rendering to Phase 2. Do not render natural-language `rule` assets as `.codex/rules/*.rules`. Add BDD coverage that demonstrates the distinction.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - Claude `rule` asset rendering does not use `CLAUDE.md` as the primary target when `.claude/rules/*.md` is appropriate.
    - Codex `rule` handling does not create `.codex/rules/*.rules`.
    - Tests assert Codex execution-policy rules are separate from natural-language guidance.
    - `npm test` passes.
  </acceptance_criteria>
  <done>Rule assets have runtime-aware Phase 1 behavior aligned with user decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Update user-facing docs for .aof workspace model</name>
  <files>README.md, schemas/aof.schema.json</files>
  <read_first>README.md, schemas/aof.schema.json, .planning/phases/01-aof-workspace-model/01-CONTEXT.md</read_first>
  <action>Update README DSL/usage sections to describe `.aof/aof.config.json`, `.aof/aof.lock.json`, `.aof/assets/<kind>/<id>/`, `overrides/claude.json`, `overrides/codex.json`, `aof migrate`, and generated `.claude/`/`.codex/` output boundaries. Document the `rule` kind and explicitly distinguish Codex `AGENTS.md` guidance from future `.codex/rules/*.rules` execution policy. Ensure examples use kind-specific filenames (`SKILL.md`, `COMMAND.md`, `AGENT.md`, `RULE.md`). Keep root `aof.config.json` documentation only as legacy compatibility.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - README documents `.aof/aof.config.json` as authoritative.
    - README documents `aof migrate` and says root `aof.config.json` remains legacy.
    - README documents BDD tests as the standard for new user-facing behavior.
    - README does not imply `.codex/rules/*.rules` is the natural-language rule target.
  </acceptance_criteria>
  <done>Documentation matches the Phase 1 workspace and runtime guidance model.</done>
</task>

<task type="auto">
  <name>Task 3: Run full Phase 1 verification sweep</name>
  <files>test/integration/cli.feature, test/integration/cli.mjs, test/adapters.test.mjs</files>
  <read_first>.planning/phases/01-aof-workspace-model/01-CONTEXT.md, .planning/phases/01-aof-workspace-model/01-RESEARCH.md, .planning/codebase/TESTING.md</read_first>
  <action>Run the full verification suite after implementation. If a BDD scenario or unit test is missing for any new behavior introduced in Plans 01-03, add the missing scenario/test before declaring completion. Ensure no temporary checkpoint files remain in `.planning/phases/01-aof-workspace-model/`.</action>
  <verify>npm run test:unit; npm test</verify>
  <acceptance_criteria>
    - `npm run test:unit` passes.
    - `npm test` passes.
    - Phase directory has no `*-DISCUSS-CHECKPOINT.json` or transient planning checkpoint files.
    - Every new user-facing behavior introduced in Phase 1 has a BDD scenario.
  </acceptance_criteria>
  <done>Phase 1 implementation has full unit and BDD verification.</done>
</task>

</tasks>

<verification>
- [ ] `npm run test:unit`
- [ ] `npm test`
- [ ] BDD scenarios prove rule/guidance behavior and Codex policy distinction.
- [ ] README and schema align with implemented behavior.
</verification>

<success_criteria>
- `rule` guidance semantics are represented and tested for Claude and Codex.
- Codex execution-policy `.rules` files remain explicitly separate and deferred.
- Documentation explains `.aof/` workspace ownership and migration.
- Full unit and BDD suites pass.
- Plans cover every Phase 1 requirement and every trackable context decision.
</success_criteria>

<output>
After completion, create `.planning/phases/01-aof-workspace-model/01-03-SUMMARY.md`.
</output>
