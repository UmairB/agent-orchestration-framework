# Phase 17 Plan 17-01: Inquirer Prompt Foundation

**Status:** Complete
**Created:** 2026-05-09

## Goal

Replace AOF's typed-line prompt implementation with an `@inquirer/prompts` foundation while preserving deterministic automation and BDD inputs.

## Scope

1. Add `@inquirer/prompts` to package dependencies.
2. Replace runtime selection with a checkbox prompt.
3. Replace future item selection with a checkbox prompt.
4. Replace confirmation with an Inquirer confirm prompt.
5. Keep `AOF_TEST_*` environment inputs working for integration tests.
6. Add non-TTY guardrails that direct automation to use explicit flags.

## Verification

- `npm run test:unit`
- `npm test`
- `npm run test:integration:ps`
- Manual `init --codex` smoke

## Result

Completed in commit `281e04a`.
