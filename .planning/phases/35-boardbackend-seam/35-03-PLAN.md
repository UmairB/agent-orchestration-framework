---
phase: 35
name: BoardBackend Seam
plan: 03
title: Execution Capability Gating And Tests
status: planned
requirements:
  - BACK-02
  - BACK-03
  - BACK-04
must_haves:
  - D-06 capability strings are the routing contract
  - D-07 null backend implements the same four methods
  - D-08 unit tests only for null backend routing
  - D-09 retest routing and unsupported capability behavior only
---

# 35-03 Plan: Execution Capability Gating And Tests

## Goal

Move task assignment provider checks to backend capabilities and prove the seam with the null backend.

## Tasks

1. Update `src/board-execution.mjs`.
   - Resolve `options.provider ?? "gsd"` via `resolveBackend()`.
   - Check `backend.capabilities.has("assignTask")`.
   - Preserve existing GSD assignment records, commands, and messages.

2. Add focused tests.
   - `resolveBackend("gsd")` returns the real backend and exposes required methods.
   - `resolveBackend("missing")` rejects with `BACKEND_UNSUPPORTED`.
   - `assignTaskToAgent(..., { provider: "null" })` rejects through capability gating without touching GSD tools.
   - Existing assignment tests continue to pass.

3. Update docs/comments only where useful.
   - Mark the seam internal and unstable for v1.7.

## Verification

- `node scripts/check-sdk-boundary.mjs`
- `node scripts/supply-chain-audit.mjs`
- `npm run test:unit`
- `npm test`

