---
phase: 02-runtime-rendering-and-lock-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/adapters.mjs
  - src/fs.mjs
  - src/lock.mjs
  - src/render-plan.mjs
  - test/adapters.test.mjs
  - test/render-plan.test.mjs
  - scripts/test-unit.mjs
requirements:
  - REND-01
  - REND-02
  - REND-03
  - REND-04
  - CLI-03
  - CLI-04
autonomous: true
user_setup: []
must_haves:
  truths:
    - "D-01 and D-02: .aof/aof.lock.json is a generated manifest that records generated file path, source asset id/kind, target runtime, content hash, and generation timestamp."
    - "D-07 and D-08: Generated files include small format-appropriate generated-by markers, while lock entries remain authoritative for ownership."
    - "D-11 and D-12: Multiple Codex rule assets targeting the same AGENTS.md are merged deterministically by asset id with labeled sections."
    - "D-13: aof apply --dry-run shows an action plan with reasons."
    - "D-14: Dry-run output classifies each affected file as create, update, delete, skip, or drift-warning."
    - "D-15: Dry-run output includes file path, runtime, source asset, and reason."
    - "D-16: Dry-run runs the same ownership, hash, drift, and stale-file analysis as real apply."
    - "D-17: Dry-run writes no runtime files, deletes no stale files, and does not update .aof/aof.lock.json."
    - "D-18: Dry-run may report the would-be lock manifest summary, but persisted lock state remains unchanged."
  artifacts:
    - path: "src/render-plan.mjs"
      provides: "Desired output generation, destination grouping, hash calculation, and action classification"
      exports: ["createRenderPlan", "planApplyActions"]
    - path: "src/lock.mjs"
      provides: "Lock manifest read/write helpers and file-entry hash utilities"
      exports: ["readLock", "writeLock", "hashContent"]
    - path: "test/render-plan.test.mjs"
      provides: "Unit coverage for render planning, hashing, drift, stale, and dry-run action decisions"
  key_links:
    - from: "src/adapters.mjs"
      to: "src/render-plan.mjs"
      via: "rendered desired outputs instead of immediate writes"
      pattern: "createRenderPlan|renderDesiredOutputs"
    - from: "src/render-plan.mjs"
      to: "src/lock.mjs"
      via: "lock entry hash comparison"
      pattern: "hashContent|readLock"
---

<objective>
Build the render-plan and lock-manifest foundation for Phase 2.

Purpose: Separate desired output generation from filesystem mutation so real apply and dry-run use identical ownership, hash, drift, and stale-file analysis.
Output: render/action plan module, lock manifest helpers, generated markers, deterministic Codex `AGENTS.md` merging, and unit coverage.
</objective>

<execution_context>
@C:/Source/umair/aof/.codex/get-shit-done/workflows/execute-plan.md
@C:/Source/umair/aof/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-runtime-rendering-and-lock-state/02-CONTEXT.md
@.planning/phases/02-runtime-rendering-and-lock-state/02-RESEARCH.md
@.planning/phases/01-aof-workspace-model/01-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@src/adapters.mjs
@src/dsl.mjs
@src/fs.mjs
@src/model.mjs
@src/workspace.mjs
@test/adapters.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lock manifest helpers</name>
  <files>src/lock.mjs, src/fs.mjs, test/render-plan.test.mjs, scripts/test-unit.mjs</files>
  <read_first>src/fs.mjs, src/workspace.mjs, test/workspace.test.mjs, scripts/test-unit.mjs, .planning/phases/02-runtime-rendering-and-lock-state/02-CONTEXT.md</read_first>
  <action>Create `src/lock.mjs` with helpers for Phase 2 lock state. Include `hashContent(content)` using SHA-256 with a stable `sha256:` prefix, `readLock(lockPath)` that returns `null` for a missing lock and throws clear errors for invalid JSON, and `writeLock(lockPath, manifest)` that writes formatted JSON through existing filesystem helpers. Add any needed `src/fs.mjs` helpers for safe optional file reads and file existence checks without weakening existing `readJson` behavior. Add `test/render-plan.test.mjs` with initial unit tests for hash stability, missing-lock reads, and lock write/read roundtrip. Register the test file in `scripts/test-unit.mjs`.</action>
  <verify>npm run test:unit</verify>
  <acceptance_criteria>
    - `hashContent("same")` returns the same `sha256:` value across calls.
    - Missing lock files are handled as no prior ownership state.
    - Invalid lock JSON fails with a clear file-specific error.
    - Lock write/read roundtrip preserves `version`, `files`, and `frameworks` fields.
    - `npm run test:unit` passes.
  </acceptance_criteria>
  <done>Lock manifest primitives are available and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Generate desired outputs without writing files</name>
  <files>src/adapters.mjs, src/render-plan.mjs, test/adapters.test.mjs, test/render-plan.test.mjs</files>
  <read_first>src/adapters.mjs, src/model.mjs, test/adapters.test.mjs, .planning/phases/02-runtime-rendering-and-lock-state/02-RESEARCH.md</read_first>
  <action>Refactor rendering so desired outputs can be produced in memory before any writes occur. Keep `applyConfig()` behavior compatible for callers while introducing a lower-level desired-output function used by the new render planner. Each desired output must include absolute path, project-relative path where applicable, runtime, source resource id/kind, rendered content, and content hash. Add generated-by markers to rendered Markdown/frontmatter formats where format allows. Preserve existing resource body rendering semantics and runtime override behavior.</action>
  <verify>npm run test:unit</verify>
  <acceptance_criteria>
    - Desired outputs can be generated without writing `.claude/` or `.codex/` files.
    - Existing adapter tests for skills, commands, agents, rules, runtime filters, and overrides still pass.
    - Generated skill/command/agent/rule output contains a small AOF generated marker.
    - Desired output metadata includes source resource id/kind and runtime.
    - `npm run test:unit` passes.
  </acceptance_criteria>
  <done>Rendering can be planned before filesystem mutation.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic output grouping and action classification</name>
  <files>src/render-plan.mjs, src/adapters.mjs, test/render-plan.test.mjs, test/adapters.test.mjs</files>
  <read_first>src/render-plan.mjs, src/adapters.mjs, src/lock.mjs, .planning/phases/02-runtime-rendering-and-lock-state/02-CONTEXT.md</read_first>
  <action>Implement grouping for desired outputs that target the same path. For Codex `rule` resources targeting the same `AGENTS.md`, merge them into one deterministic generated document sorted by asset id with labeled sections. For unsupported non-rule collisions, fail with a clear error rather than silently overwriting. Implement `planApplyActions()` that compares desired outputs, prior lock entries, and current filesystem hashes to classify actions as `create`, `update`, `delete`, `skip`, or `drift-warning`. Keep action objects rich enough for CLI reporting: action, path, runtime, source asset, and reason.</action>
  <verify>npm run test:unit</verify>
  <acceptance_criteria>
    - Multiple Codex rule assets targeting the same `AGENTS.md` produce one file with deterministic section order by asset id.
    - Non-rule output path collisions fail clearly.
    - Action planning detects creates for missing desired files.
    - Action planning detects updates for owned files whose current hash matches prior lock but desired hash changed.
    - Action planning detects drift-warning for owned files whose current hash differs from prior lock.
    - Action planning detects delete for stale owned files whose current hash matches prior lock.
    - Action planning skips deleting stale drifted files.
    - `npm run test:unit` passes.
  </acceptance_criteria>
  <done>Render planning produces deterministic desired outputs and classified actions.</done>
</task>

</tasks>

<verification>
- [ ] `npm run test:unit`
- [ ] Unit tests cover lock helpers, desired output generation, generated markers, deterministic Codex `AGENTS.md` merge, drift detection, and stale-file planning.
</verification>

<success_criteria>
- Render planning is separated from file writing.
- `.aof/aof.lock.json` manifest helpers exist.
- Desired output metadata is sufficient to write lock file entries.
- Dry-run and real apply can share action classification.
- Codex `AGENTS.md` merges are deterministic and covered by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-rendering-and-lock-state/02-01-SUMMARY.md`.
</output>
