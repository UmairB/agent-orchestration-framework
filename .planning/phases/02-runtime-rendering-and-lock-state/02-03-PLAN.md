---
phase: 02-runtime-rendering-and-lock-state
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/cli.mjs
  - src/frameworks.mjs
  - src/lock.mjs
  - README.md
  - test/integration/cli.feature
  - test/integration/cli.mjs
  - test/render-plan.test.mjs
  - .planning/ROADMAP.md
  - .planning/STATE.md
requirements:
  - REND-01
  - REND-02
  - REND-03
  - REND-04
  - FRAM-04
  - CLI-03
  - CLI-04
autonomous: true
user_setup: []
must_haves:
  truths:
    - "D-05 and D-06: Phase 2 records managed framework intent in lock state only and does not implement full framework install-result tracking or new GSD execution behavior."
    - "D-01 through D-18: Plans and verification must cover every Phase 2 context decision."
    - "Global decision: BDD tests are required for all new functionality."
  artifacts:
    - path: ".aof/aof.lock.json"
      provides: "Versioned generated manifest with file entries and framework intent after apply"
      contains: "frameworks"
    - path: "README.md"
      provides: "User-facing generated-output, dry-run, lock, drift, and pruning documentation"
      contains: "aof.lock.json"
    - path: "test/integration/cli.feature"
      provides: "End-to-end BDD coverage for Phase 2 behavior"
      contains: "lock"
  key_links:
    - from: "src/cli.mjs"
      to: "src/frameworks.mjs"
      via: "framework package declarations become lock intent, not installer execution"
      pattern: "packages|frameworks"
    - from: "README.md"
      to: ".planning/phases/02-runtime-rendering-and-lock-state/02-CONTEXT.md"
      via: "documents generated-output and lock decisions"
      pattern: "generated output|drift|dry-run"
---

<objective>
Complete Phase 2 by recording managed framework intent in lock state, documenting generated-output behavior, and running a full verification sweep.

Purpose: Close FRAM-04, CLI-04, and the documentation/testing obligations after the render/apply behavior is implemented.
Output: framework intent lock entries, docs, final BDD coverage, full verification, and planning state annotations.
</objective>

<execution_context>
@C:/Source/umair/aof/.codex/get-shit-done/workflows/execute-plan.md
@C:/Source/umair/aof/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-runtime-rendering-and-lock-state/02-CONTEXT.md
@.planning/phases/02-runtime-rendering-and-lock-state/02-RESEARCH.md
@.planning/phases/02-runtime-rendering-and-lock-state/02-02-SUMMARY.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/TESTING.md
@src/cli.mjs
@src/frameworks.mjs
@src/lock.mjs
@README.md
@test/integration/cli.feature
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record managed framework intent in lock state</name>
  <files>src/cli.mjs, src/frameworks.mjs, src/lock.mjs, test/integration/cli.feature, test/integration/cli.mjs, test/render-plan.test.mjs</files>
  <read_first>src/frameworks.mjs, src/cli.mjs, src/lock.mjs, .planning/phases/02-runtime-rendering-and-lock-state/02-CONTEXT.md</read_first>
  <action>Extend lock manifest generation so config `packages` are represented as managed framework intent during `aof apply`. For GSD, record id, source/package reference, selected target runtimes, local/global scope, and intent metadata. Do not execute framework install commands from `apply`; keep installer behavior in `aof install gsd` for Phase 3. Add tests or BDD scenarios proving package declarations appear in `.aof/aof.lock.json` after real apply and do not appear as a dry-run side effect.</action>
  <verify>npm run test:unit && npm test</verify>
  <acceptance_criteria>
    - A config with `packages: [{ id: "gsd", source: "npm:get-shit-done-cc@latest", runtimes: [...] }]` records framework intent in `.aof/aof.lock.json`.
    - Framework intent includes runtimes and local/global scope.
    - `aof apply` does not run `npx` or install GSD.
    - Dry-run reports framework intent summary if useful but does not write lock state.
    - Tests cover framework intent lock state.
  </acceptance_criteria>
  <done>FRAM-04 is satisfied without expanding Phase 3 install behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Document generated output, lock state, and dry-run behavior</name>
  <files>README.md, test/integration/cli.feature</files>
  <read_first>README.md, .planning/phases/02-runtime-rendering-and-lock-state/02-CONTEXT.md, .planning/phases/02-runtime-rendering-and-lock-state/02-RESEARCH.md</read_first>
  <action>Update README to explain `.claude/` and `.codex/` as generated output from `.aof/`, generated markers, `.aof/aof.lock.json` manifest entries, drift warnings, force overwrite behavior, stale-file pruning, side-effect-free `aof apply --dry-run`, and framework intent in lock state. Keep docs concise and usage-first. Ensure docs do not imply UI execution or full GSD install management is in Phase 2.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - README documents runtime folders as generated output, not source of truth.
    - README documents drift detection and explicit force overwrite.
    - README documents stale owned-file pruning.
    - README documents dry-run as side-effect-free.
    - README documents framework intent lock entries without claiming full install execution in apply.
    - `npm test` passes.
  </acceptance_criteria>
  <done>User-facing docs match Phase 2 behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Run Phase 2 verification and close planning state</name>
  <files>test/integration/cli.feature, test/integration/cli.mjs, test/render-plan.test.mjs, test/adapters.test.mjs, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>.planning/phases/02-runtime-rendering-and-lock-state/02-CONTEXT.md, .planning/phases/02-runtime-rendering-and-lock-state/02-RESEARCH.md, .planning/codebase/TESTING.md, test/integration/cli.feature</read_first>
  <action>Run a final verification sweep for Phase 2. Confirm every requirement REND-01, REND-02, REND-03, REND-04, FRAM-04, CLI-03, and CLI-04 is covered by at least one BDD or unit test. Add missing BDD steps if needed for lock JSON file entries, absence of files, stale pruning, and framework intent. Update ROADMAP Phase 2 plan list with wave dependency notes and cross-cutting constraints. Update STATE.md to record Phase 2 planned with three plans and ready to execute. Do not mark Phase 2 implemented; only planning is complete.</action>
  <verify>npm run test:unit; npm test</verify>
  <acceptance_criteria>
    - `npm run test:unit` passes.
    - `npm test` passes.
    - BDD coverage exists for every user-facing Phase 2 behavior.
    - ROADMAP Phase 2 lists Plans 01-03 with wave dependency notes.
    - STATE.md says Phase 2 is planned and ready to execute.
    - No `*-DISCUSS-CHECKPOINT.json` or transient planning checkpoint files remain.
  </acceptance_criteria>
  <done>Phase 2 plans and verification coverage are complete and ready for execution.</done>
</task>

</tasks>

<verification>
- [ ] `npm run test:unit`
- [ ] `npm test`
- [ ] BDD coverage confirms generated output, dry-run, lock state, drift, stale pruning, and framework intent behavior.
- [ ] README, ROADMAP, and STATE align with implemented Phase 2 behavior and planning state.
</verification>

<success_criteria>
- Lock state records managed framework intent.
- README documents Phase 2 behavior accurately.
- Full unit and integration suites pass.
- Plans cover all Phase 2 requirements and all trackable decisions from `02-CONTEXT.md`.
- Phase 2 is ready for `$gsd-execute-phase 2`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-rendering-and-lock-state/02-03-SUMMARY.md`.
</output>
