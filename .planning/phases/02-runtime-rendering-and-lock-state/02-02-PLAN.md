---
phase: 02-runtime-rendering-and-lock-state
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/cli.mjs
  - src/adapters.mjs
  - src/render-plan.mjs
  - src/lock.mjs
  - test/integration/cli.feature
  - test/integration/cli.mjs
  - test/render-plan.test.mjs
requirements:
  - REND-01
  - REND-02
  - REND-03
  - REND-04
  - CLI-03
  - CLI-04
autonomous: true
user_setup: []
must_haves:
  truths:
    - "D-03 and D-04: Drifted previously generated files are warned about and skipped by default; overwriting requires an explicit force flag."
    - "D-09 and D-10: Stale files are pruned only when the prior lock says AOF generated them and they have not drifted."
    - "D-13: aof apply --dry-run reports an action plan with reasons."
    - "D-14: Dry-run classifies each affected file as create, update, delete, skip, or drift-warning."
    - "D-15: Dry-run output includes file path, runtime, source asset, and reason."
    - "D-16: Dry-run uses the same ownership, hash, drift, and stale-file analysis as apply."
    - "D-17: Dry-run writes no runtime files, deletes no stale files, and does not update .aof/aof.lock.json."
    - "D-18: Dry-run may report the would-be lock manifest summary while leaving the persisted lock unchanged."
    - "CLI-03: User-facing apply output explains what files will be written, skipped, deleted, or warned about."
  artifacts:
    - path: "src/cli.mjs"
      provides: "Apply command integration for action plans, dry-run reporting, drift warnings, force flag, and lock writes"
      contains: "dryRun"
    - path: "test/integration/cli.feature"
      provides: "BDD coverage for apply dry-run, drift, pruning, and lock behavior"
      contains: "dry-run"
  key_links:
    - from: "src/cli.mjs"
      to: "src/render-plan.mjs"
      via: "apply command builds action plan before writes"
      pattern: "planApplyActions|createRenderPlan"
    - from: "src/cli.mjs"
      to: "src/lock.mjs"
      via: "apply reads prior lock and writes new manifest after successful apply"
      pattern: "readLock|writeLock"
---

<objective>
Wire render planning into `aof apply` with dry-run reporting, lock updates, drift protection, and stale-file pruning.

Purpose: Make the CLI behavior match Phase 2 user decisions while preserving current rendering behavior for normal creates/updates.
Output: `aof apply` action plan reporting, side-effect-free dry-run, real lock writes, drift skip/force behavior, pruning, and BDD coverage.
</objective>

<execution_context>
@C:/Source/umair/aof/.codex/get-shit-done/workflows/execute-plan.md
@C:/Source/umair/aof/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-runtime-rendering-and-lock-state/02-CONTEXT.md
@.planning/phases/02-runtime-rendering-and-lock-state/02-RESEARCH.md
@.planning/phases/02-runtime-rendering-and-lock-state/02-01-SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@src/cli.mjs
@src/adapters.mjs
@src/render-plan.mjs
@src/lock.mjs
@src/workspace.mjs
@test/integration/cli.feature
@test/integration/cli.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate action planning into apply command</name>
  <files>src/cli.mjs, src/adapters.mjs, src/render-plan.mjs, src/lock.mjs, test/integration/cli.feature, test/integration/cli.mjs</files>
  <read_first>src/cli.mjs, src/workspace.mjs, src/render-plan.mjs, src/lock.mjs, test/integration/cli.mjs</read_first>
  <action>Update `applyCommand()` to build the shared render/action plan before writing. Read the prior lock from `.aof/aof.lock.json` using `workspacePaths(targetDir).lockPath`. Execute create/update/delete actions only when not dry-run. Print each action with enough context to satisfy CLI-03: action, path, runtime when available, source resource when available, and reason. Preserve existing `applyConfig()` export compatibility if tests or callers use it directly, but route CLI apply through the new action planner.</action>
  <verify>npm run test:unit && npm test</verify>
  <acceptance_criteria>
    - `aof apply` still renders `.aof/` assets into Claude and Codex runtime folders.
    - CLI output differentiates create/update/delete/skip/drift-warning rather than printing only `write`.
    - Existing BDD scenarios that assert rendered files still pass.
    - `npm run test:unit` and `npm test` pass.
  </acceptance_criteria>
  <done>`aof apply` uses the shared action plan for real writes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement dry-run as side-effect-free action analysis</name>
  <files>src/cli.mjs, src/render-plan.mjs, test/integration/cli.feature, test/integration/cli.mjs, test/render-plan.test.mjs</files>
  <read_first>.planning/phases/02-runtime-rendering-and-lock-state/02-CONTEXT.md, src/cli.mjs, src/render-plan.mjs, test/integration/cli.feature</read_first>
  <action>Make `aof apply --dry-run` run the same action classification as real apply and print the same action plan with reasons. Ensure dry-run does not write runtime files, delete stale files, or write `.aof/aof.lock.json`. Add BDD coverage for dry-run create output and no files written. Add a lock-state assertion proving dry-run does not create or update `.aof/aof.lock.json`; extend BDD steps only as needed.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - `aof apply --dry-run --codex` prints create actions and reasons for generated Codex files.
    - Dry-run does not create `.codex/`, `.claude/`, or `.aof/aof.lock.json`.
    - Dry-run and apply share the same action classification code path.
    - BDD coverage exists for side-effect-free dry-run behavior.
    - `npm test` passes.
  </acceptance_criteria>
  <done>Dry-run previews the exact apply analysis without side effects.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce drift protection and stale-file pruning</name>
  <files>src/cli.mjs, src/render-plan.mjs, src/lock.mjs, test/integration/cli.feature, test/integration/cli.mjs, test/render-plan.test.mjs</files>
  <read_first>src/cli.mjs, src/render-plan.mjs, src/lock.mjs, test/integration/cli.mjs, .planning/phases/02-runtime-rendering-and-lock-state/02-CONTEXT.md</read_first>
  <action>Implement drift protection for previously generated files. If current file content no longer matches the prior lock hash, emit a drift-warning and skip overwriting by default. Add an explicit force option, preferably reusing `--force`, to overwrite drifted owned files when the user opts in. Implement stale-file pruning for prior lock entries that are no longer desired and whose current hash still matches the prior lock. Do not delete stale drifted files. Add BDD scenarios for manual edit drift skip, force overwrite, and non-drifted stale-file pruning after an asset is removed or retargeted.</action>
  <verify>npm run test:unit && npm test</verify>
  <acceptance_criteria>
    - Manual edits to a generated file are detected on the next apply.
    - Default apply warns and skips a drifted file.
    - `aof apply --force` overwrites a drifted owned file and updates lock state.
    - Removing or retargeting a source asset prunes stale owned files only when current content matches the prior lock hash.
    - Stale drifted files are not deleted by default.
    - Unit and BDD tests cover these cases.
  </acceptance_criteria>
  <done>Apply protects user edits and cleans up owned stale output.</done>
</task>

</tasks>

<verification>
- [ ] `npm run test:unit`
- [ ] `npm test`
- [ ] BDD scenarios cover dry-run, real lock writes, drift skip, force overwrite, and stale pruning.
</verification>

<success_criteria>
- `aof apply` writes generated files through the shared action planner.
- `aof apply --dry-run` reports exact planned actions without side effects.
- `.aof/aof.lock.json` is updated after successful real apply.
- Drifted generated files are skipped unless force is explicit.
- Stale owned non-drifted files are pruned.
- User-facing output explains actions and reasons.
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-rendering-and-lock-state/02-02-SUMMARY.md`.
</output>
