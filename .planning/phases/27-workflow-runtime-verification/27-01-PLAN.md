# Phase 27 Wave 1 Plan: Coverage Audit And Live UAT

**Status:** Complete
**Requirements:** HARD-01, HARD-02, HARD-03, HARD-04, HARD-05

## Tasks

1. Confirm prior BDD coverage maps to hardening requirements.
2. Create a disposable project with a shared workflow, Claude command wrapper, Codex skill wrapper, and `{{skills.*}}` reference.
3. Run `aof project validate` and `aof assets apply` against the disposable project.
4. Inspect generated outputs for runtime-specific workflow and skill paths.

## Completion Notes

- Live UAT passed. The disposable project validated, applied, and rendered the expected `.claude/aof/workflows/*`, `.codex/aof/workflows/*`, `.claude/commands/*`, and `.codex/skills/*` outputs.
