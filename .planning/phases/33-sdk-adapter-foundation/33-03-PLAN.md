# Plan 33-03: Fixture And Adapter Tests

## Goal

Seed Phase 36's SDK fixture surface and prove the adapter contract in focused unit tests.

## Tasks

1. Add `test/fixtures/gsd-sdk/v17-active/` with `roadmap-analyze.stdout.json` and `state-load.stdout.txt`.
2. Add `test/fixtures/gsd-sdk/README.md` with the capture recipe.
3. Add unit tests for happy-path adapter calls, structured mismatch results, dispatch logging, error wrapping, and the SDK boundary check.

## Acceptance Criteria

- D-17, D-18, and D-19 are covered.
- `npm run test:unit` includes adapter tests.
- `node scripts/supply-chain-audit.mjs` passes after dependency changes.

