---
phase: 40
name: Board Attachment And Sync UAT
plan: 01
title: Phase 39 Straggler Completion — Baseline Evidence And Git Commit
status: pending
type: evidence-capture
wave: 1
depends_on: []
autonomous: true
requirements:
  - BOARD-01
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04
  - FIX-01
files_modified:
  - .aof/.gitignore
  - .aof/boards/coordination/BOARD.json
  - .aof/boards/coordination/tasks/phase-39.json
  - .aof/boards/coordination/tasks/phase-40.json
  - .aof/boards/coordination/tasks/phase-41.json
  - .aof/boards/coordination/tasks/phase-42.json
  - .aof/boards/coordination/tasks/phase-43.json
  - .aof/boards/coordination/executions/phase-39.json
  - .aof/boards/coordination/executions/phase-40.json
  - .aof/skills/aof-board-milestone-bridge/SKILL.md
  - .planning/milestones/v1.7-UAT.md
  - .planning/phases/39-board-dogfood-requirements-and-live-state-baseline/39-BASELINE-OUTPUT.md
  - .planning/phases/39-board-dogfood-requirements-and-live-state-baseline/39-UAT-LOG.md
  - src/internal-skills.mjs
  - scripts/uat-v1.7-legacy-board-migration.ps1
  - scripts/uat-v1.7-sdk-board.ps1
must_haves:
  - D-01 All untracked .aof/ and planning/phases/39-* files committed as durable project state
  - D-05 39-BASELINE-OUTPUT.md with one section per command (human then JSON)
  - D-06 Commands: list, show, validate, index, doctor (human + JSON where supported)
  - D-07 aof assets validate run to confirm bridge skill NOT in rendered output
  - D-08 39-UAT-LOG.md created as milestone-wide v1.8 finding tracker
  - D-10 UAT log schema: ID, Phase discovered, Command/surface, Severity, Summary, Repro steps, Expected behavior, Actual behavior, Status, Resolution
  - D-13 Bridge skill leak check: if aof-board-milestone-bridge appears in rendered asset output, log as UAT-01 critical
---

# 40-01 Plan: Phase 39 Straggler Completion

## Goal

Produce the two missing Phase 39 deliverables (`39-BASELINE-OUTPUT.md` and `39-UAT-LOG.md`) and commit all untracked board/skill/planning files that were never committed in Phase 39's empty execute step.

## Context

Phase 39's execute step ran in 111 ms with `planResults: []` — no artifacts were produced. The following are still untracked per the git status snapshot:

- `.aof/.gitignore` — cache-protection gitignore
- `.aof/boards/` — entire boards directory including coordination board and all 5 tasks/executions
- `.aof/skills/` — bridge skill (internal only; not in `aof.config.json` resources)
- `.planning/milestones/v1.7-UAT.md` — v1.7 UAT log
- `.planning/phases/39-board-dogfood-requirements-and-live-state-baseline/` — phase context only
- `scripts/uat-v1.7-legacy-board-migration.ps1` — v1.7 UAT scripts
- `scripts/uat-v1.7-sdk-board.ps1`
- `src/internal-skills.mjs` — bridge skill loader

The current live board is already in a clean state (`status: "synced"`, `gsd.milestone.id: "v1.8"`, 5 tasks, `syncedAt: "2026-05-19T08:27:55.474Z"`). This wave captures that clean state as evidence before Wave 2 modifies it.

## Tasks

### 1. Run baseline CLI captures (read-only)

Run each command below and collect output. These are all read-only — they do not modify any board files.

```
node bin/aof.mjs boards list
node bin/aof.mjs boards list --json
node bin/aof.mjs boards show coordination
node bin/aof.mjs boards show coordination --json
node bin/aof.mjs boards validate
node bin/aof.mjs boards validate --json
node bin/aof.mjs boards index
node bin/aof.mjs boards doctor coordination
node bin/aof.mjs boards doctor coordination --json
```

Note: `aof boards validate --json` is known to return a `BOARD_INDEX_STALE` warning because the index has not been rebuilt yet. This is expected — run `aof boards index` first, then re-run validate to get a clean result. Capture BOTH the pre-index and post-index validate output in the baseline file.

Also run the bridge skill leak check:
```
node bin/aof.mjs assets validate
```
Confirm `aof-board-milestone-bridge` does NOT appear in the validated resources list. If it does, log `UAT-01` in the UAT log with severity critical.

### 2. Write `39-BASELINE-OUTPUT.md`

Write to `.planning/phases/39-board-dogfood-requirements-and-live-state-baseline/39-BASELINE-OUTPUT.md`.

Structure:
```markdown
# Phase 39 Baseline CLI Output
**Captured:** <date>
**Board state:** status=synced, milestone=v1.8, tasks=5
**Board path:** .aof/boards/coordination/BOARD.json

## aof boards list (human)
<output>

## aof boards list --json
<output>

## aof boards show coordination (human)
<output>

## aof boards show coordination --json
<output>

## aof boards validate (pre-index, human)
<output>

## aof boards validate --json (pre-index)
<output>

## aof boards index (human)
<output>

## aof boards validate (post-index, human)
<output>

## aof boards validate --json (post-index)
<output>

## aof boards doctor coordination (human)
<output>

## aof boards doctor coordination --json
<output>

## aof assets validate (bridge skill leak check)
<output>
### Result: PASS — aof-board-milestone-bridge not in rendered output
```

**Acceptance:** Every command section has real captured output. BOARD_INDEX_STALE disappears after `aof boards index` is run.

### 3. Write `39-UAT-LOG.md`

Write to `.planning/phases/39-board-dogfood-requirements-and-live-state-baseline/39-UAT-LOG.md`.

This is the **milestone-wide v1.8 UAT finding tracker**. All future phases (40, 41, 42, 43) append to this file in-place. Phase 43 derives the final UAT REPORT from it.

Structure:
```markdown
---
status: open
milestone: v1.8
---

# v1.8 UAT Log

**Started:** <date>
**Milestone:** v1.8 AOF Boards Dogfood UAT
**Status:** Open

## Findings

| ID | Phase | Command/Surface | Severity | Summary | Status |
|----|-------|----------------|----------|---------|--------|
| (none at baseline) | | | | | |

## Finding Detail

(none at baseline — clean state confirmed)

## Resolution History

(none)
```

If the bridge skill leak check in task 1 finds a leak, add it as `UAT-01`:

```markdown
| UAT-01 | 39 | aof assets validate | critical | Bridge skill aof-board-milestone-bridge appears in rendered asset output | open |
```

With full detail block:
```markdown
### UAT-01: Bridge skill leaks into rendered asset output

**Phase discovered:** 39
**Command/surface:** `aof assets validate`
**Severity:** critical
**Repro steps:**
1. Run `node bin/aof.mjs assets validate`
2. Observe rendered resources list

**Expected:** `aof-board-milestone-bridge` is internal-only and must NOT appear as a resource that renders to `.claude/` or `.codex/`
**Actual:** Skill appears in resource list
**Status:** open
**Resolution:** Fix in Phase 43
```

**Acceptance:** UAT log is created with correct schema. If no baseline findings, the table is empty with a note that baseline state was clean.

### 4. Commit all untracked files

Stage and commit:
- `.aof/.gitignore`
- `.aof/boards/` (entire tree)
- `.aof/skills/` (entire tree — bridge skill)
- `.planning/milestones/v1.7-UAT.md`
- `.planning/phases/39-board-dogfood-requirements-and-live-state-baseline/` (context + two new baseline files)
- `scripts/uat-v1.7-legacy-board-migration.ps1`
- `scripts/uat-v1.7-sdk-board.ps1`
- `src/internal-skills.mjs`

Commit message: `chore: commit phase 39 baseline — board state, bridge skill, and UAT evidence`

**Acceptance:** `git status` shows none of the above as untracked. The `.aof/cache/` directory (if it exists) is NOT committed — it must be in `.aof/.gitignore`.

## Verification

- `39-BASELINE-OUTPUT.md` exists with all sections populated.
- `39-UAT-LOG.md` exists with correct schema.
- `git status` shows `.aof/boards/`, `.aof/skills/`, and `src/internal-skills.mjs` as tracked.
- `.aof/cache/` is NOT in the commit (gitignore protects it).
- Bridge skill leak check result is documented in the baseline file.
