# Phase 25 Wave 2 Plan: Validation And Global References

**Status:** Complete
**Requirements:** REF-03, REF-04

## Goal

Reject missing, malformed, unsupported, and runtime-incompatible asset references before any generated output is written.

## Tasks

1. Build the asset reference index from local resources/workflows plus referenced global resources/workflows.
2. Validate resource body/path/override placeholders against the runtime scope that will render the text.
3. Validate workflow body placeholders against workflow runtimes.
4. Add diagnostics for invalid namespace, invalid ID, missing reference, and runtime mismatch.
5. Add unit coverage for local, override, and referenced global cases.

## Verification

- `npm run test:unit` passed on 2026-05-14.

## Completion Notes

- Validation now builds one asset reference index from local plus referenced global skills/workflows.
- Resource bodies, file-backed primary bodies, runtime overrides, and workflow bodies are checked before apply writes.
- Diagnostics cover unsupported namespaces, missing references, and runtime mismatches.
