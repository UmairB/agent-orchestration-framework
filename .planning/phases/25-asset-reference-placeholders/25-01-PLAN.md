# Phase 25 Wave 1 Plan: Reference Parser And Rendering

**Status:** Complete
**Requirements:** REF-01, REF-02

## Goal

Add shared parsing/indexing helpers and expand valid skill/workflow placeholders during runtime rendering.

## Tasks

1. Create a shared `src/asset-references.mjs` helper for strict placeholder extraction, runtime path generation, and reference indexes.
2. Expand `{{skills.*}}` and `{{workflows.*}}` in resource primary bodies and runtime override bodies.
3. Expand the same placeholders in workflow bodies.
4. Add unit coverage for Codex and Claude path expansion.

## Verification

- `npm run test:unit` passed on 2026-05-14.

## Completion Notes

- Added `src/asset-references.mjs` for strict parsing, indexing, runtime path expansion, and shared runtime scope helpers.
- Wired resource, override, and workflow body expansion through the renderer.
- Added adapter unit coverage for Claude/Codex skill and workflow path expansion.
