# Phase 26 Wave 1 Plan: Editable Workflow-Backed Resource Model

**Status:** Complete
**Requirements:** UI-01, UI-02

## Tasks

1. Extend editable resource normalization/loading/saving with `workflow`, `argumentHint`, `arguments`, and `argumentOverrides`.
2. Save workflow-backed resources without a primary body path when no explicit wrapper body is provided.
3. Keep simple asset argument validation active only for simple mode.
4. Add config-editor unit tests for workflow-backed resource saves and simple-mode argument rejection.

## Verification

- `npm run test:unit` passed on 2026-05-14.

## Completion Notes

- Editable resources now preserve workflow-backed metadata and can save wrapper-only resources without forcing a body path.
- Simple-mode argument validation ignores absent optional fields and blocks explicit argument metadata.
- Added config-editor and setup UI API unit coverage for workflow-backed resource saves.
