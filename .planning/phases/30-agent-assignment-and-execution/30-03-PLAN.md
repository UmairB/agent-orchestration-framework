# Plan 30-03: Lifecycle Guarantees And BDD Coverage

## Goal

Prove the assignment lifecycle behaves predictably across common UAT paths.

## Tasks

1. Add BDD scenarios for successful task assignment.
2. Add BDD scenarios for unknown agent rejection.
3. Add BDD scenarios for missing GSD phase ref rejection.
4. Add BDD scenarios for execution status updates.
5. Ensure execution records contain the GSD ceremony commands needed for a future runner or UI console.
6. Verify validation/index behavior remains compatible with execution summary fields.

## Acceptance Criteria

- BDD covers assignment, failure diagnostics, and execution state persistence.
- Execution files are written in the board folder, not `.planning`.
- Board task status and execution status remain separate but synchronized.
- Generated records include enough resume context for failed or blocked work.

## Verification

- Run focused board feature integration tests.
- Run `npm test` if focused checks pass.
