---
phase: "34"
plan: "34-04"
type: "verification"
wave: 3
depends_on:
  - "34-01"
  - "34-02"
  - "34-03"
files_modified:
  - test/boards.test.mjs
  - test/integration/features/boards.feature
  - test/integration/steps/boards.steps.mjs
  - test/integration/steps/boards.steps.ps1
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-03
  - SYNC-04
  - SYNC-05
  - SYNC-06
  - SYNC-07
  - LIFE-01
  - LIFE-02
  - LIFE-03
  - LIFE-04
  - LIFE-05
  - MIG-02
  - MIG-04
must_haves:
  - "D-01: Tests cover missing --milestone structured failure."
  - "D-02: Tests and boundary checks prove SDK-only sync."
  - "D-03: Tests cover SDK failure as typed failure."
  - "D-04: Tests cover dry-run JSON preview with no writes."
  - "D-05: Tests cover idempotent re-sync."
  - "D-06: Tests cover binding readiness as the task gate."
  - "D-07: Tests cover supported binding statuses."
  - "D-08: Tests cover normalized fingerprint stability."
  - "D-09: Tests cover partial failure not marking synced."
  - "D-10: Tests cover persisted SDK version."
  - "D-11: Tests cover one-candidate auto-bind."
  - "D-12: Tests cover ambiguous no-write repair."
  - "D-13: Tests cover validation warning."
  - "D-14: Tests cover sync command normalization."
  - "D-15: UI build and assertions cover compact milestone plus binding text."
  - "D-16: UI assertions cover short drift/error labels."
  - "D-17: API tests cover existing route shapes."
  - "D-18: UI review confirms no richer layout/control changes."
---

# Plan 34-04: Tests And Verification

## Goal

Prove the Phase 34 lifecycle migration through focused unit, integration, and UI build verification.

## Tasks

1. Update `test/boards.test.mjs` for:
   - typed SDK sync success;
   - missing milestone structured error;
   - attach unknown milestone no-write behavior;
   - dry-run preview actions;
   - idempotent re-sync;
   - v1.6 repair auto-bind and ambiguous no-write paths;
   - `validateBoards` warning for missing milestone id.
2. Add or update test support fixtures/classes so board tests inject the SDK adapter through its public options instead of importing `@gsd-build/sdk` elsewhere.
3. Update `test/integration/features/boards.feature` and shared steps for the new sync/attach output, binding fields, and JSON/dry-run expectations.
4. Preserve Node and PowerShell step parity by keeping feature steps generic and shared.
5. Run:
   - `node scripts/check-sdk-boundary.mjs`
   - `npm run test:unit`
   - `npm test`
   - `npm run ui:build`

## Decision Coverage

- D-01: Tests cover missing `--milestone` structured failure.
- D-02: Tests and boundary checks prove SDK-only sync.
- D-03: Tests cover SDK failure as typed failure.
- D-04: Tests cover dry-run JSON preview with no writes.
- D-05: Tests cover idempotent re-sync.
- D-06: Tests cover binding readiness as the task gate.
- D-07: Tests cover supported binding statuses.
- D-08: Tests cover normalized fingerprint stability.
- D-09: Tests cover partial failure not marking synced.
- D-10: Tests cover persisted SDK version.
- D-11: Tests cover one-candidate auto-bind.
- D-12: Tests cover ambiguous no-write repair.
- D-13: Tests cover validation warning.
- D-14: Tests cover sync command normalization.
- D-15: UI build and assertions cover compact milestone plus binding text.
- D-16: UI assertions cover short drift/error labels.
- D-17: API tests cover existing route shapes.
- D-18: UI review confirms no richer layout/control changes.

## Acceptance Criteria

- All Phase 34 requirements have direct automated coverage or explicit deferred coverage where Phase 36/38 owns the wider surface.
- D-01 through D-18 are verified through focused unit, integration, and UI build checks in this plan.
- Existing board/task behavior outside GSD-backed sync remains green.
- UI build passes after compact status text changes.
