---
status: testing
milestone: v1.7
source:
  - .planning/milestones/v1.7-ROADMAP.md
  - .planning/milestones/v1.7-REQUIREMENTS.md
  - .planning/milestones/v1.7-MILESTONE-AUDIT.md
started: 2026-05-18T08:37:31+01:00
updated: 2026-05-18T14:30:18+01:00
---

# UAT: v1.7 Typed GSD SDK Backend

## Current Test

number: 6
name: SDK boundary and dependency safety checks
expected: |
  From the repository root, run:
  `node scripts/supply-chain-audit.mjs`
  `node scripts/check-sdk-boundary.mjs`
  `npm run test:unit`
  `npm test`

  All commands should pass.
awaiting: user response

## Tests

### 1. SDK-backed board sync, doctor, and assignment
expected: In a disposable project, board creation starts pending attachment, milestone attach succeeds, SDK fixture sync creates two phase tasks, doctor reports a healthy board with SDK drift warning, and assigning phase-30 to builder records a complete SDK execution.
result: pass
script: scripts/uat-v1.7-sdk-board.ps1
project: C:\Users\Umair\AppData\Local\Temp\aof-v17-uat-fd77c07ab4634cbc9835220ba95d32ee

### 2. Structured JSON remediation for missing milestone argument
expected: Running `boards sync delivery --json` before passing `--milestone` fails with JSON containing `ok: false`, `code: "MILESTONE_MISSING_ARG"`, and `next: "aof boards sync delivery --milestone <milestone-id>"`.
result: pass
observed: |
  `aof boards sync delivery --json` returned JSON with `ok: false`,
  `code: "MILESTONE_MISSING_ARG"`, and
  `next: "aof boards sync delivery --milestone <milestone-id>"`.

### 3. v1.6 board migration repair and sync
expected: A v1.6-shaped board with `roadmapPath` and no milestone id is repaired into an attached v1.7 board, then syncs without creating duplicate phase tasks.
result: pass
script: scripts/uat-v1.7-legacy-board-migration.ps1
project: C:\Users\Umair\AppData\Local\Temp\aof-v17-legacy-uat-c76734e00774458e90ae82b9b95c895c
observed: |
  `boards repair legacy` attached the board to milestone `v1.7`.
  First sync created `phase-30` and `phase-31`; second sync reported `created: 0`.

### 4. v1.6 board doctor migration hint
expected: Before repair, `boards doctor legacy` reports `WARN BOARD_MILESTONE_ID_MISSING` with a prefilled `aof boards milestone attach legacy --milestone v1.7 --roadmap docs/v1-6-roadmap.md` next command; JSON output exposes the same code and next hint.
result: pass
observed: User reported pass.

### 5. GSD milestone creation coordination handoff
expected: Board creation starts a visible `$gsd-new-milestone` handoff; milestone status does not suggest answering before the runtime handoff has started; milestone answer records coordination turns once there is a runtime session; attach and sync bind the completed milestone roadmap.
result: partial
observed: |
  Initial UAT exposed that milestone status suggested `boards milestone answer`
  before the runtime handoff had started. That misleading status guidance was
  fixed. Follow-up scope remains for using GSD `agent_skills` injection to teach
  the relevant GSD agents how to call AOF board attachment/sync commands after
  milestone creation.

### 6. SDK boundary and dependency safety checks
expected: `node scripts/supply-chain-audit.mjs`, `node scripts/check-sdk-boundary.mjs`, `npm run test:unit`, and `npm test` all pass from the repository root.
result: [pending]

## Summary

total: 6
passed: 5
issues: 0
pending: 1
skipped: 0
blocked: 0

## Gaps

- Test 5 needs a targeted GSD `agent_skills` injection design instead of an AOF-side autosync command.
