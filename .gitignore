node_modules/
coverage/
ui/dist/
graphify-out/
.tmp-npm-cache/
.claude/
.codex/
.aof-test/
.aof/aof.memory.index.json
*.sqlite
*.sqlite-journal
.DS_Store
*.log

# Runtime: live agent-terminal session registry (operational, not committed)
.aof/terminal-sessions.json

# The aof self-host repo is NOT itself a mesh node (milestone 23 / story 00, R4):
# a live `aof mesh *` run here persists a machine-specific record to the tracked
# .mesh/ stream, but this repo must not commit a node's presence/identity. A REAL
# node where .mesh/ IS the committed bus is a DIFFERENT repo with no such ignore.
wiki/work/.mesh/
